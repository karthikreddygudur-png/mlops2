# Runtime dependencies for the inference service (installed into the Docker image).
# CPU-only torch wheels are pulled via the extra index URL in the Dockerfile.
torch==2.5.1
torchvision==0.20.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pillow==11.0.0
numpy==2.1.3
python-multipart==0.0.20
prometheus-fastapi-instrumentator==7.0.0
pyyaml==6.0.2
